import component from './src/JsonView.vue'
import { withInstall } from '../../../utils/withInstall'
  
const WxyJsonView = withInstall(component)
  
export {
  WxyJsonView
}
  
export default WxyJsonView